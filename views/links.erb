<h1>bookmarks </h1>
<h2>Welcome <%= User.first.email if User.first != nil %></h2>
<ul id=links>
  <% @links.each do |link| %>
    <%= link.title %> - <%= link.url %> - <%= link.tags[0].name %> <br>
  <% end %>
</ul>
<a href='/links/new'>Add link</a>
